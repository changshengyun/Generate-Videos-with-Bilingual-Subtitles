interface:
  display_name: "MVP Implementation Gate"
  short_description: "Build and verify an approved vertical-slice MVP"
  default_prompt: "Implement the approved vertical-slice MVP and verify it against the canonical project documents."
policy:
  allow_implicit_invocation: true
